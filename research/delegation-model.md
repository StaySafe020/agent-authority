# Agent Authority — Delegation Security Model

## Overview

Today's research focused on the security properties required for a multi-agent authority system.

The core problem remains:

How can we prove that an autonomous agent has legitimate authority to perform an action, while preserving the full chain of delegation?

```text
Human → Agent A → Agent B → Action